using BlockGame.world.item;

namespace BlockGame.util.stuff;

public class RecipeRegistry : Registry<Recipe> {
    // recipes don't need parallel property arrays
    // they're just data objects lol
}